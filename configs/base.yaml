exp_name: "vsfc_mapping"
exp_id: ""
output_root: "results"

solver_name: "milp_baseline"
metrics_name: "network_metrics"
topology_name: "placeholder_topology"

run_kwargs:
  time_limit: 60
  enable_warm_start: false

plot:
  x: "metric"
  y: "mean"
  yerr: "std"
  title: "vSFC Metrics Overview"
